% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get-params.R
\name{get_params}
\alias{get_params}
\title{Get every hyperparameter of a kernel, in its natural (constrained) space}
\usage{
get_params(kernel)
}
\arguments{
\item{kernel}{A kernel object.}
}
\value{
A named numeric vector.
}
\description{
Flattens the kernel's hyperparameter tree and unwraps every value. The
result is suitable to inspect a kernel's current configuration. For the
values an optimiser should actually vary, see \code{\link[=get_trainable_params]{get_trainable_params()}},
which excludes parameters frozen via \code{\link[=freeze]{freeze()}}.

If a hyperparameter name is shared by more than one sub-kernel (e.g.
\code{se_kernel(1) + periodic_kernel(length_scale = 2, period = 3)}, both
having a \code{length_scale}), the shared entries are returned under
disambiguated names (\code{"length_scale (se_kernel)"},
\code{"length_scale (periodic_kernel)"}) rather than as ambiguous duplicate
names in the returned vector -- see \code{\link[=.disambiguate_param_names]{.disambiguate_param_names()}}. This
does not affect \code{\link[=kupdate]{kupdate()}}/\code{\link[=freeze]{freeze()}}, which continue to match by the
bare name, structurally, everywhere it occurs in the tree (their
documented, intentional "propagate to every occurrence" behaviour -- see
\code{\link[=kupdate]{kupdate()}}). Call \code{\link[=check_param_names]{check_param_names()}} if you want to know, ahead of
time, whether a name collision in a kernel you built is likely
intentional (shared HP) or a typo.
}
\examples{
get_params(se_kernel(length_scale = 2))
}
