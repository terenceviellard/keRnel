% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{input_specific_param_kernel}
\alias{input_specific_param_kernel}
\title{Per-input hyperparameters}
\usage{
input_specific_param_kernel(inner, input_size, param_axes = NULL)
}
\arguments{
\item{inner}{A kernel object, or a numeric value (coerced via
\code{\link[=as_kernel]{as_kernel()}}).}

\item{input_size}{A positive integer: the expected number of points in
\code{x1} (an error is raised if \code{nrow(x1)} does not match).}

\item{param_axes}{\code{NULL} (auto-detect, the default), or a character
vector naming the hyperparameters that vary per input point.}
}
\value{
An object of class \code{input_specific_param_kernel}/
\code{wrapper_kernel}/\code{kernel}.
}
\description{
Wraps \code{inner} so that every point of \code{x1} gets its own hyperparameter
value, rather than a single value shared across all of \code{x1}. Build
\code{inner} with a hyperparameter of length \code{input_size} to vary it per
point of \code{x1} (see \code{\link[=slice_kernel]{slice_kernel()}}); a value left as a single scalar is
shared across all points of \code{x1}. A typical use is heteroscedastic noise:
\code{input_specific_param_kernel(white_noise_kernel(noise = c(0.1, 0.5, 0.2)), input_size = 3)} gives each of the 3 points of \code{x1} its own noise level.

Parameters are only varied along \code{x1}; \code{x2} is always evaluated as a
whole against each sliced point of \code{x1} in turn, so take care when \code{x1}
and \code{x2} differ.

\strong{Symmetry caveat}: in the auto-covariance case (\code{x2 = NULL}), entry
\verb{(i, j)} is computed with point \code{i}'s hyperparameter value and entry
\verb{(j, i)} with point \code{j}'s -- the result is only a symmetric (and
positive semi-definite) covariance matrix if the varied hyperparameter
acts on the \emph{diagonal only}, as \code{\link[=white_noise_kernel]{white_noise_kernel()}}'s \code{noise} does.
Varying a cross-covariance hyperparameter (e.g. a \code{length_scale})
per point produces an asymmetric matrix, which is not a valid GP
covariance.

Which hyperparameters vary per input point is the same explicit contract
as \code{\link[=batch_kernel]{batch_kernel()}}'s (see \code{\link[=.resolve_axes]{.resolve_axes()}}): auto-detected by default
(\code{param_axes = NULL}, every sliceable multi-valued hyperparameter must
then have exactly \code{input_size} values), or named explicitly via
\code{param_axes}, leaving other multi-valued hyperparameters to other
wrappers' axes.
}
\examples{
k <- input_specific_param_kernel(
  white_noise_kernel(noise = c(0.1, 0.5, 0.2)),
  input_size = 3
)
evaluate(k, c(0, 1, 2))
}
