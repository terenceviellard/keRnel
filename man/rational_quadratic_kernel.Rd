% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stationary-kernels.R
\name{rational_quadratic_kernel}
\alias{rational_quadratic_kernel}
\title{Rational quadratic kernel}
\usage{
rational_quadratic_kernel(
  length_scale,
  alpha,
  length_scale_parametrisation = log_exp_parametrisation(),
  alpha_parametrisation = log_exp_parametrisation(),
  distance_function = squared_euclidean_distance,
  engine = dense_engine()
)
}
\arguments{
\item{length_scale}{A strictly positive numeric value.}

\item{alpha}{A strictly positive numeric value.}

\item{length_scale_parametrisation}{A \link{parametrisation} for
\code{length_scale}. Defaults to \code{\link[=log_exp_parametrisation]{log_exp_parametrisation()}}.}

\item{alpha_parametrisation}{A \link{parametrisation} for \code{alpha}. Defaults to
\code{\link[=log_exp_parametrisation]{log_exp_parametrisation()}}.}

\item{distance_function}{A distance function, or a shortcut name (see
\code{\link[=resolve_distance_function]{resolve_distance_function()}}). Defaults to
\code{\link[=squared_euclidean_distance]{squared_euclidean_distance()}}.}

\item{engine}{A computation engine. Defaults to \code{\link[=dense_engine]{dense_engine()}}.}
}
\value{
An object of class \code{rational_quadratic_kernel}/
\code{stationary_kernel}/\code{kernel}.
}
\description{
\deqn{k(x_1, x_2) = \left(1 + \frac{d}{2 \alpha \cdot \mathrm{length\_scale}^2}\right)^{-\alpha}}

where \eqn{d} is \code{distance_function} (squared Euclidean by default).
\code{length_scale} and \code{alpha} are constrained to stay strictly positive.
}
\examples{
k <- rational_quadratic_kernel(length_scale = 1, alpha = 1)
evaluate(k, c(0, 1, 2))
}
