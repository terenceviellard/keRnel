% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{slice_kernel_nd}
\alias{slice_kernel_nd}
\title{Extract one Cartesian combination of a multi-axis batch wrapper's hyperparameters}
\usage{
slice_kernel_nd(kernel, combo, axis_spec, batch_size)
}
\arguments{
\item{kernel}{A kernel object.}

\item{combo}{A named integer vector (axis name -> current 1-based index).}

\item{axis_spec}{A named list (hyperparameter name -> character vector of
axis names), the contract resolved by \code{.resolve_axes_multi()}.}

\item{batch_size}{A named integer vector (axis name -> axis size).}
}
\value{
A new kernel object, with every axis hyperparameter sliced at the
combination \code{combo} implies.
}
\description{
The multi-axis counterpart of \code{\link[=slice_kernel]{slice_kernel()}}. \code{combo} gives one index
per wrapper axis (a named integer vector over \code{names(batch_size)}); each
hyperparameter named in \code{axis_spec} is replaced by the element of its
\verb{$wrapped} vector at the flattened (column-major) index implied by
\code{combo} restricted to that hyperparameter's own declared axes -- see the
flattening convention documented in \code{\link[=batch_kernel]{batch_kernel()}}.
}
\keyword{internal}
