% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{log_kernel}
\alias{log_kernel}
\title{Logarithm of a kernel}
\usage{
log_kernel(inner)
}
\arguments{
\item{inner}{A kernel object, or a numeric value (coerced via
\code{\link[=as_kernel]{as_kernel()}}).}
}
\value{
An object of class \code{log_kernel}/\code{wrapper_kernel}/\code{kernel}.
}
\description{
\deqn{k(x_1, x_2) = \log(k_{\mathrm{inner}}(x_1, x_2))}

Note: \code{k_inner}'s output must be strictly positive everywhere for the
result to be defined; this is not checked at construction (it generally
cannot be, since it depends on the inputs \code{evaluate()} is later called
with), only at evaluation time (\code{NaN}, with an R warning, as usual for
\code{log()} of a non-positive value).
}
\examples{
k <- log_kernel(variance_kernel(variance = 2))
evaluate(k, c(0, 1, 2))
}
