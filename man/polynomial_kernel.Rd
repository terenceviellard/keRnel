% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dot-product-kernels.R
\name{polynomial_kernel}
\alias{polynomial_kernel}
\title{Polynomial kernel}
\usage{
polynomial_kernel(
  degree,
  gamma,
  constant,
  gamma_parametrisation = log_exp_parametrisation(),
  distance_function = dot_product,
  engine = dense_engine()
)
}
\arguments{
\item{degree}{A positive integer. Not optimisable via \code{\link[=get_params]{get_params()}} or
\code{\link[=kupdate]{kupdate()}} -- create a new kernel instance to change it.}

\item{gamma}{A strictly positive numeric value.}

\item{constant}{A numeric value (any real value): the independent term.
Not optimisable via \code{\link[=get_params]{get_params()}}.}

\item{gamma_parametrisation}{A \link{parametrisation} for \code{gamma}. Defaults to
\code{\link[=log_exp_parametrisation]{log_exp_parametrisation()}}.}

\item{distance_function}{A distance function, or a shortcut name (see
\code{\link[=resolve_distance_function]{resolve_distance_function()}}). Defaults to \code{\link[=dot_product]{dot_product()}}.}

\item{engine}{A computation engine. Defaults to \code{\link[=dense_engine]{dense_engine()}}.}
}
\value{
An object of class \code{polynomial_kernel}/\code{dot_product_kernel}/
\code{kernel}.
}
\description{
\deqn{k(x_1, x_2) = (\gamma \cdot \langle x_1, x_2 \rangle + c)^{\mathrm{degree}}}
}
\examples{
k <- polynomial_kernel(degree = 2, gamma = 1, constant = 1)
evaluate(k, c(0, 1, 2))
}
