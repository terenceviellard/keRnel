% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stationary-kernels.R
\name{white_noise_kernel}
\alias{white_noise_kernel}
\title{White noise kernel}
\usage{
white_noise_kernel(
  noise,
  noise_parametrisation = log_exp_parametrisation(),
  engine = dense_engine()
)
}
\arguments{
\item{noise}{A strictly positive numeric value.}

\item{noise_parametrisation}{A \link{parametrisation} for \code{noise}. Defaults to
\code{\link[=log_exp_parametrisation]{log_exp_parametrisation()}}.}

\item{engine}{A computation engine. Defaults to \code{\link[=dense_engine]{dense_engine()}}.}
}
\value{
An object of class \code{white_noise_kernel}/\code{stationary_kernel}/
\code{kernel}.
}
\description{
Returns \code{noise} on the diagonal (where \code{x1 == x2}) and \code{0} elsewhere:

\deqn{k(x_1, x_2) = \mathrm{noise} \cdot \mathbb{1}[x_1 = x_2]}

\code{noise} is constrained to be \emph{strictly} positive by the default
\code{\link[=log_exp_parametrisation]{log_exp_parametrisation()}}, which raises an error on a zero or negative
value rather than silently producing a degenerate kernel. Simply omit
\code{white_noise_kernel()} from a sum of kernels if no noise is desired.
}
\examples{
k <- white_noise_kernel(noise = 0.1)
evaluate(k, c(0, 1, 2))
}
