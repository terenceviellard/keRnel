% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernel-interactions.R
\name{kernel_interactions}
\alias{kernel_interactions}
\title{Build the full interaction expansion of a list of kernels}
\usage{
kernel_interactions(kernels, max_order = length(kernels))
}
\arguments{
\item{kernels}{A list of kernel objects.}

\item{max_order}{An integer between \code{1} and \code{length(kernels)}. Defaults
to \code{length(kernels)} (the full expansion).}
}
\value{
A single kernel object (a composition of \code{\link[=sum_kernel]{sum_kernel()}} and
\code{\link[=product_kernel]{product_kernel()}}).
}
\description{
For kernels \verb{k1, k2, k3}, the full expansion (\code{max_order} = 3, the
default) is:

\deqn{k_1 + k_2 + k_3 + k_1 k_2 + k_1 k_3 + k_2 k_3 + k_1 k_2 k_3}

i.e. the sum of every product of 1 to \code{max_order} of the input kernels,
taken without repetition -- \code{2^n - 1} terms for \code{n} kernels at the
default \code{max_order = length(kernels)}. Set \code{max_order} lower (e.g. \code{2})
to limit the expansion to low-order interactions only.
}
\examples{
k1 <- se_kernel(length_scale = 1)
k2 <- linear_kernel(slope_var = 1)
k3 <- periodic_kernel(length_scale = 1, period = 2)
k <- kernel_interactions(list(k1, k2, k3))
evaluate(k, c(0, 1, 2))

# Limit to pairwise interactions (k1+k2+k3+k1*k2+k1*k3+k2*k3, no triple product)
kernel_interactions(list(k1, k2, k3), max_order = 2)
}
