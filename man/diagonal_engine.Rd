% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engines.R
\name{diagonal_engine}
\alias{diagonal_engine}
\title{Diagonal computation engine}
\usage{
diagonal_engine()
}
\value{
An object of class \code{diagonal_engine}/\code{engine}.
}
\description{
Computes only the diagonal of the covariance matrix, i.e. \code{k(x1[i], x2[i])}
for each aligned pair of points, as a vector of length \code{nrow(X1)} --
\code{O(n)} instead of \code{O(n^2)}. Useful when only the diagonal is needed, e.g.
the predictive variance of a Gaussian Process.

\code{diagonal_engine()} is a deliberate addition: the optimisation it
captures (avoiding an \code{O(n^2)} computation when only the diagonal is
needed) is purely algebraic, independent of any particular compilation
or hardware backend.

Requires \code{x1} and \code{x2} to have the same number of points.
}
\examples{
k <- se_kernel(length_scale = 1, engine = diagonal_engine())
evaluate(k, c(0, 1, 2), c(0, 1, 2))
}
