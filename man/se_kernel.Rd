% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stationary-kernels.R
\name{se_kernel}
\alias{se_kernel}
\alias{rbf_kernel}
\title{Squared Exponential (RBF) kernel}
\usage{
se_kernel(
  length_scale,
  length_scale_parametrisation = log_exp_parametrisation(),
  distance_function = squared_euclidean_distance,
  engine = dense_engine()
)

rbf_kernel(
  length_scale,
  length_scale_parametrisation = log_exp_parametrisation(),
  distance_function = squared_euclidean_distance,
  engine = dense_engine()
)
}
\arguments{
\item{length_scale}{A strictly positive numeric value (or vector, for a
batched kernel -- see \code{\link[=batch_kernel]{batch_kernel()}}).}

\item{length_scale_parametrisation}{A \link{parametrisation} for
\code{length_scale}. Defaults to \code{\link[=log_exp_parametrisation]{log_exp_parametrisation()}}.}

\item{distance_function}{A distance function, or a shortcut name (see
\code{\link[=resolve_distance_function]{resolve_distance_function()}}). Defaults to
\code{\link[=squared_euclidean_distance]{squared_euclidean_distance()}}.}

\item{engine}{A computation engine. Defaults to \code{\link[=dense_engine]{dense_engine()}}.}
}
\value{
An object of class \code{se_kernel}/\code{stationary_kernel}/\code{kernel}.
}
\description{
The Squared Exponential kernel (also known as "RBF" or "Gaussian"):

\deqn{k(x_1, x_2) = \exp\left(-\frac{d(x_1, x_2)}{2 \cdot \mathrm{length\_scale}^2}\right)}

where \eqn{d} is \code{distance_function} (squared Euclidean by default).
\code{length_scale} is constrained to stay strictly positive.
}
\examples{
k <- se_kernel(length_scale = 1)
evaluate(k, c(0, 1, 2))
}
