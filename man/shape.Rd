% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distance-matrix.R
\name{shape}
\alias{shape}
\title{Elementwise shape function of a stationary kernel}
\usage{
shape(kernel, d)
}
\arguments{
\item{kernel}{A kernel object.}

\item{d}{A numeric value, vector, or matrix of distances.}
}
\value{
The same shape as \code{d}.
}
\description{
For a stationary kernel, \code{pairwise(kernel, x1, x2)} is always
\code{shape(kernel, distance_function(x1, x2))}: a scalar function of the
distance alone. Expressing it this way lets \code{\link[=pairwise_matrix]{pairwise_matrix()}} apply it
to a whole distance matrix at once -- R vectorises elementwise math
(\code{exp()}, \code{^}, ...) over a matrix for free, so this is the one piece each
stationary kernel must provide to get a fast, vectorised
\code{\link[=evaluate]{evaluate()}} for free, on top of its scalar \code{\link[=pairwise]{pairwise()}} (which remains
the reference definition of correctness, exercised directly in tests).

A custom kernel's \code{shape()} method is entirely optional -- a
\code{stationary_kernel}/\code{dot_product_kernel} that only defines \code{\link[=pairwise]{pairwise()}}
remains correct, just without this speed-up (see
\code{vignette("b-intermediate")}, "Writing your own kernel", and
\code{vignette("c-expert")}, "Vectorisation internals").
}
\examples{
k <- se_kernel(length_scale = 1)
shape(k, c(0, 1, 4)) # exp(-0.5 * d / length_scale^2)
}
