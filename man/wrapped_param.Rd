% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrapped-param.R
\name{wrapped_param}
\alias{wrapped_param}
\title{Wrapped hyperparameter}
\usage{
wrapped_param(
  value,
  parametrisation = log_exp_parametrisation(),
  frozen = FALSE,
  sliceable = TRUE
)
}
\arguments{
\item{value}{A numeric value or vector, in the constrained (natural) space.}

\item{parametrisation}{A \link{parametrisation} object. Defaults to
\code{\link[=log_exp_parametrisation]{log_exp_parametrisation()}} (the most common case: a strictly positive
hyperparameter).}

\item{frozen}{Logical, whether the parameter is excluded from optimisation.}

\item{sliceable}{Logical, whether a multi-valued vector for this parameter
may be interpreted as one-value-per-batch-element. Defaults to \code{TRUE}.}
}
\value{
An object of class \code{wrapped_param}.
}
\description{
The atomic unit of an optimisable kernel hyperparameter. It bundles
together:
\itemize{
\item \code{wrapped}: the value in the unconstrained ("wrapped") space.
\item \code{parametrisation}: the \link{parametrisation} object used to \code{wrap()}/
\code{unwrap()} it.
\item \code{frozen}: whether this parameter should be excluded from optimisation
(see \code{\link[=freeze]{freeze()}} and \code{\link[=get_trainable_params]{get_trainable_params()}}).
\item \code{sliceable}: whether a multi-valued vector for this parameter indexes a
batch/block axis (and may therefore be sliced by \code{\link[=batch_kernel]{batch_kernel()}} and
friends, see \code{\link[=slice_kernel]{slice_kernel()}}), or something else entirely -- e.g.
\code{\link[=ard_kernel]{ard_kernel()}}'s \code{length_scales}, whose entries index \emph{input
dimensions}, never batch elements. Non-sliceable parameters are ignored
by batch-axis auto-detection and rejected if named explicitly in a
\code{batch_axes}-style argument.
}

\code{wrapped_param} bundles a hyperparameter's value and its parametrisation
into a single, S3-dispatchable object, rather than keeping them as
separate fields linked only by a naming convention. This makes generic
tree-walking functions (\code{get_params()}, \code{kupdate()}, \code{slice_kernel()}...)
robust: they dispatch on \code{inherits(x, "wrapped_param")} rather than
guessing field names.

The \emph{intent} behind \code{frozen} -- a parameter that participates in the
computation normally but must never be optimised -- is fully captured by
\code{frozen = TRUE} here, with no need for a dedicated parametrisation class.
\code{sliceable} follows the same philosophy for the batching axis: metadata
carried directly by the parameter itself, no parallel structure to keep
in sync.
}
\examples{
p <- wrapped_param(2.5)
unwrap_param(p)

p_frozen <- wrapped_param(1, frozen = TRUE)
p_frozen$frozen
}
