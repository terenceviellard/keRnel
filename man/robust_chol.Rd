% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linalg.R
\name{robust_chol}
\alias{robust_chol}
\title{Robust Cholesky factor (adaptive jitter), dimnames preserved}
\usage{
robust_chol(mat, pen_diag = 1e-10)
}
\arguments{
\item{mat}{A symmetric (quasi-)positive-definite numeric matrix.}

\item{pen_diag}{Initial jitter added to the diagonal; multiplied by 10 on
each \code{chol()} failure.}
}
\value{
The upper-triangular factor \code{R} such that \code{mat + jitter = t(R) \%*\% R},
with the same \code{dimnames()} as \code{mat} and an attribute \code{pen_diag_used}
(the jitter that was actually retained).
}
\description{
Like \code{\link[=chol]{chol()}}, but with a recovery path: a (quasi-)singular \code{mat} no
longer makes it fail outright -- a small jitter is added to the
diagonal, retried with a 10x larger jitter each time \code{chol()} still
fails, until it succeeds. \code{dimnames()} are preserved either way.
}
\examples{
k <- se_kernel(length_scale = 1)
K <- evaluate(k, c(0, 1, 2))
robust_chol(K)
}
