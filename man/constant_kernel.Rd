% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/other-kernels.R
\name{constant_kernel}
\alias{constant_kernel}
\title{Constant kernel}
\usage{
constant_kernel(
  value = 1,
  value_parametrisation = identity_parametrisation(),
  engine = dense_engine()
)
}
\arguments{
\item{value}{A finite numeric value. Defaults to \code{1}.}

\item{value_parametrisation}{A \link{parametrisation} for \code{value}. Defaults to
\code{\link[=identity_parametrisation]{identity_parametrisation()}}.}

\item{engine}{A computation engine. Defaults to \code{\link[=dense_engine]{dense_engine()}}.}
}
\value{
An object of class \code{constant_kernel}/\code{kernel}.
}
\description{
Returns the same constant value regardless of the inputs:

\deqn{k(x_1, x_2) = \mathrm{value}}

Unlike \code{\link[=variance_kernel]{variance_kernel()}}, \code{value} is unconstrained: it may be any
finite real number (negative included). It is still stored as a
\code{\link[=wrapped_param]{wrapped_param()}} (with \code{\link[=identity_parametrisation]{identity_parametrisation()}} by default), for
architectural consistency with every other hyperparameter -- so \code{value}
is visible to \code{\link[=get_params]{get_params()}}/\code{\link[=kupdate]{kupdate()}}/\code{\link[=freeze]{freeze()}} like any other
optimisable hyperparameter, and validated (finite, numeric) at
construction and at every \code{\link[=kupdate]{kupdate()}}.
}
\examples{
k <- constant_kernel(value = 2)
evaluate(k, c(0, 1, 2))
}
