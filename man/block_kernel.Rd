% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{block_kernel}
\alias{block_kernel}
\title{Block covariance matrix, with cross-block terms}
\usage{
block_kernel(inner, nb_blocks, batch_over_inputs = FALSE, block_axes = NULL)
}
\arguments{
\item{inner}{A kernel object, or a numeric value (coerced via
\code{\link[=as_kernel]{as_kernel()}}).}

\item{nb_blocks}{A positive integer: the number of blocks.}

\item{batch_over_inputs}{Logical, whether \code{x1}/\code{x2} are themselves
per-block 3-dimensional arrays. Defaults to \code{FALSE}. See
\code{\link[=batch_kernel]{batch_kernel()}}.}

\item{block_axes}{\code{NULL} (auto-detect, the default), or a character
vector naming the hyperparameters that vary per block. Same contract
as \code{\link[=batch_kernel]{batch_kernel()}}'s \code{batch_axes} (see \code{\link[=.resolve_axes]{.resolve_axes()}}).}
}
\value{
An object of class \code{block_kernel}/\code{wrapper_kernel}/\code{kernel}.
}
\description{
Wraps \code{inner} so that \code{\link[=evaluate]{evaluate()}} produces a full \verb{(n1 * nb_blocks) x (n2 * nb_blocks)} block matrix, including the cross-block (block \code{i},
block \code{j}, \code{i != j}) terms -- unlike \code{\link[=block_diag_kernel]{block_diag_kernel()}}, which leaves
them at \code{0}.

Computing a cross-block term requires \code{inner} to read a \strong{pair} of
hyperparameter values at once (one per block side), via
\code{\link[=pair_slice_kernel]{pair_slice_kernel()}}: build \code{inner} with a hyperparameter of length
\code{nb_blocks} to vary it per block, exactly as for \code{\link[=batch_kernel]{batch_kernel()}} (see
\code{\link[=slice_kernel]{slice_kernel()}}).

Most kernels are not designed to interpret a \emph{pair} of values per
hyperparameter (\code{\link[=se_kernel]{se_kernel()}}, for instance, expects a single scalar
\code{length_scale}) -- using one of those with per-block hyperparameter
values would silently produce a mathematically meaningless result.
\code{\link[=feature_kernel]{feature_kernel()}} is, deliberately, designed for exactly this (it
already reads a length-2 \code{length_scale}/\code{variance} pair, one value per
"side"). To catch misuse early rather than silently computing garbage,
\code{block_kernel()} raises an error if \code{inner} has a per-block
hyperparameter (length \code{nb_blocks}) but is not explicitly marked
block-compatible (\code{attr(inner, "block_compatible")} -- see
\code{\link[=feature_kernel]{feature_kernel()}}'s constructor). A kernel with only shared (length-1)
hyperparameters needs no such marker: every block is then identical, no
pairing is ever attempted.

Only the upper triangle of blocks is actually computed; the rest is
filled by symmetry (block \verb{(j, i)} is the transpose of block \verb{(i, j)}),
since \code{inner}'s formula is symmetric under swapping which side is
"left"/"right" together with \code{x1}/\code{x2} -- this algebraic shortcut is
expressed here as a dedicated mapping primitive, \code{map_block_symmetric()}
(layer 2 of the architecture), rather than inline in \code{evaluate()}.
}
\examples{
k <- block_kernel(
  feature_kernel(length_scale = c(1, 2), length_scale_u = 1, variance = c(1, 1)),
  nb_blocks = 2
)
evaluate(k, c(0, 1)) # 4x4, with non-zero cross-block terms
}
