% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get-params.R
\name{.flatten_params_with_class}
\alias{.flatten_params_with_class}
\title{Like \code{flatten_params()}, but also records each \code{wrapped_param}'s owning
kernel's class alongside it.}
\usage{
.flatten_params_with_class(kernel)
}
\arguments{
\item{kernel}{A kernel object.}
}
\value{
A list with \code{params} (same as \code{\link[=flatten_params]{flatten_params()}}) and \code{classes}
(a character vector, \code{class(owner)[1]} for each entry, same length and
order as \code{params}).
}
\description{
Used only to build human-readable, class-qualified names for
\code{\link[=get_params]{get_params()}}/\code{\link[=get_trainable_params]{get_trainable_params()}} when a hyperparameter name is
shared by sibling sub-kernels (see \code{\link[=check_param_names]{check_param_names()}}) -- deliberately
\strong{not} merged into \code{\link[=flatten_params]{flatten_params()}} itself, which is the shared
tree-walk skeleton reused by \code{\link[=kupdate]{kupdate()}}/\code{\link[=freeze]{freeze()}}/\code{\link[=sample_hps]{sample_hps()}}/
\code{.resolve_axes()}; none of those need, or should be affected by, this
extra bookkeeping.
}
\keyword{internal}
