% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stationary-kernels.R
\name{periodic_kernel}
\alias{periodic_kernel}
\title{Periodic kernel}
\usage{
periodic_kernel(
  length_scale,
  period,
  length_scale_parametrisation = log_exp_parametrisation(),
  period_parametrisation = log_exp_parametrisation(),
  distance_function = euclidean_distance,
  engine = dense_engine()
)
}
\arguments{
\item{length_scale}{A strictly positive numeric value.}

\item{period}{A strictly positive numeric value.}

\item{length_scale_parametrisation}{A \link{parametrisation} for
\code{length_scale}. Defaults to \code{\link[=log_exp_parametrisation]{log_exp_parametrisation()}}.}

\item{period_parametrisation}{A \link{parametrisation} for \code{period}. Defaults
to \code{\link[=log_exp_parametrisation]{log_exp_parametrisation()}}.}

\item{distance_function}{A distance function, or a shortcut name (see
\code{\link[=resolve_distance_function]{resolve_distance_function()}}). Defaults to \code{\link[=euclidean_distance]{euclidean_distance()}}.}

\item{engine}{A computation engine. Defaults to \code{\link[=dense_engine]{dense_engine()}}.}
}
\value{
An object of class \code{periodic_kernel}/\code{stationary_kernel}/\code{kernel}.
}
\description{
\deqn{k(x_1, x_2) = \exp\left(-\frac{2 \sin^2(\pi d / \mathrm{period})}{\mathrm{length\_scale}^2}\right)}

where \eqn{d} is \code{distance_function} (Euclidean by default). Both
\code{length_scale} and \code{period} are constrained to stay strictly positive.
}
\section{A very small \code{period}}{

An extremely small \code{period} (e.g. \code{1e-300}) does not error -- \code{sin()}
still returns a finite, well-defined value -- but the result is numerical
aliasing: \code{d / period} is so large that floating-point rounding makes
\code{sin(pi * d / period)} behave essentially like noise as \code{d} varies, rather
than encoding any meaningful periodicity. The output stays finite and
symmetric, it is just not meaningful. No threshold is enforced in
\code{evaluate()} itself, since legitimate high-frequency use (a genuinely
tiny but intentional \code{period}) must not be blocked; see
\code{dev/ameliorations/README.md} (probe A10) for the measured behaviour.
}

\examples{
k <- periodic_kernel(length_scale = 1, period = 2)
evaluate(k, c(0, 1, 2))
}
