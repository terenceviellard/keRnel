% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get-params.R
\name{.disambiguate_param_names}
\alias{.disambiguate_param_names}
\title{Disambiguate hyperparameter names shared across sibling sub-kernels}
\usage{
.disambiguate_param_names(names, classes)
}
\arguments{
\item{names}{Character vector of hyperparameter names.}

\item{classes}{Character vector, same length, the owning kernel class of
each name.}
}
\value{
\code{names}, with colliding entries qualified.
}
\description{
A bare name (\code{length_scale}) is left untouched unless another occurrence
of the same name exists elsewhere in the tree. A colliding name is
qualified with its owning kernel's class (\code{length_scale (se_kernel)},
\code{length_scale (periodic_kernel)}) -- the same class name already visible
in \code{format()}'s tree-shaped output, so the two stay consistent. If the
\emph{same} class also repeats (e.g. \code{se_kernel(1) + se_kernel(2)}, where the
class alone cannot disambiguate), occurrences within that class are
additionally numbered (\verb{length_scale (se_kernel #1)},
\verb{length_scale (se_kernel #2)}), scoped to the colliding group only --
never a global positional index over the whole tree.
}
\keyword{internal}
