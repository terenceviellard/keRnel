% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan.R
\name{eval_plan}
\alias{eval_plan}
\title{Re-evaluate a kernel against a precomputed plan}
\usage{
eval_plan(
  plan,
  kernel,
  na_action = "propagate",
  mask_variance = 1,
  ...,
  .self_covariance = NULL
)
}
\arguments{
\item{plan}{A \code{kernel_plan}, from \code{\link[=precompute]{precompute()}}.}

\item{kernel}{A kernel object with the same tree structure as the one
\code{plan} was built from.}

\item{na_action}{One of \code{"propagate"} or \code{"mask"}, as for \code{\link[=evaluate]{evaluate()}}.}

\item{mask_variance}{As for \code{\link[=evaluate]{evaluate()}}.}

\item{...}{Forwarded to \code{\link[=evaluate]{evaluate()}} for any fallback (uncached) subtree.}

\item{.self_covariance}{As for \code{\link[=evaluate]{evaluate()}}. Defaults to whether the
\code{x1}/\code{x2} originally passed to \code{\link[=precompute]{precompute()}} were themselves a
self-covariance call (\code{x2} omitted or identical to \code{x1}) -- the same
rule \code{\link[=evaluate]{evaluate()}} applies, so calling \code{eval_plan()} directly (without
going through \code{\link[=batch_kernel]{batch_kernel()}}, which always resolves and forwards
this explicitly) still masks \code{NA} correctly under \code{na_action = "mask"}.}
}
\value{
The covariance matrix (or vector, for a \code{diagonal_engine} leaf),
identical to \code{evaluate(kernel, x1, x2)} for the \code{x1}/\code{x2} \code{plan} was
built from.
}
\description{
Re-walks \code{plan} (built by \code{\link[=precompute]{precompute()}}), applying \code{\link[=shape]{shape()}} to each
cached distance instead of recomputing it -- \code{kernel} must share the
exact tree structure of the kernel the plan was built from (same classes,
same nesting; only hyperparameter values may differ, e.g. after
\code{\link[=kupdate]{kupdate()}}). \code{na_action}/\code{mask_variance}/\code{.self_covariance} reproduce
\code{\link[=evaluate]{evaluate()}}'s missing-value handling exactly, applied independently per
leaf (matching how a sum/product of kernels already combines
independently NA-handled sub-results today).
}
\seealso{
\code{\link[=precompute]{precompute()}}
}
