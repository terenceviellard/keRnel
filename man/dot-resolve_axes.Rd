% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{.resolve_axes}
\alias{.resolve_axes}
\title{Resolve which hyperparameters vary along a wrapper's batch/block axis}
\usage{
.resolve_axes(inner, n, axes, axes_arg, size_arg)
}
\arguments{
\item{inner}{A kernel object (the wrapper's sub-kernel).}

\item{n}{A positive integer: the wrapper's axis size.}

\item{axes}{\code{NULL}, or a character vector of hyperparameter names.}

\item{axes_arg, size_arg}{The wrapper's argument names, for error messages
(e.g. \code{"batch_axes"}, \code{"batch_size"}).}
}
\value{
A character vector of resolved axis names (possibly empty).
}
\description{
The single place where the "which hyperparameter is batched?" question is
answered, shared by \code{\link[=batch_kernel]{batch_kernel()}}, \code{\link[=block_diag_kernel]{block_diag_kernel()}},
\code{\link[=block_kernel]{block_kernel()}} and \code{\link[=input_specific_param_kernel]{input_specific_param_kernel()}}. Returns the axis
names as a character vector, after validating them against the kernel
tree; called both at construction time and again at \code{evaluate()} time
(a \code{\link[=kupdate]{kupdate()}} in between may have changed a value's length).

Two modes:
\itemize{
\item \code{axes = NULL} (auto-detection): every \emph{sliceable} multi-valued
\code{wrapped_param} is taken to vary along this wrapper's axis, and must
therefore have exactly \code{n} values -- any other length is an error
(silently recycling or truncating a hyperparameter vector is how
covariance matrices go quietly wrong). Parameters marked
\code{sliceable = FALSE} (e.g. \code{\link[=ard_kernel]{ard_kernel()}}'s per-\emph{dimension}
\code{length_scales}) are skipped: their length indexes something else
entirely.
\item \verb{axes = <character vector>}: only the named hyperparameters vary along
this axis; every occurrence must have length \code{n} (or 1, i.e. shared).
Other multi-valued parameters are left alone -- they belong to some
\emph{other} wrapper's axis (this is what makes nested batching with
different sizes per level work).
}
}
\keyword{internal}
