% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print.R
\name{kernel_tree}
\alias{kernel_tree}
\title{Print a kernel as an indented tree of classes and hyperparameters}
\usage{
kernel_tree(kernel, indent = 0)
}
\arguments{
\item{kernel}{A kernel object.}

\item{indent}{Internal use (recursion depth).}
}
\value{
\code{kernel}, invisibly (called for its printed output).
}
\description{
\code{format()}/\code{print()} give a single-line, composable string (e.g.
\code{"k1 + k2"}) meant to stay embeddable in error messages and plot titles.
\code{kernel_tree()} is a complementary, more verbose view: one line per node
of the \verb{$subkernels} tree, indented by nesting depth, each with its own
class name and hyperparameters -- values and \link[=freeze]{frozen} status
(via the same \code{format.wrapped_param()} used everywhere else, see
\code{\link[=wrapped_param]{wrapped_param()}}). Useful to inspect a deeply nested composite kernel at
a glance.
}
\examples{
k <- (variance_kernel(0.4) * se_kernel(length_scale = 2)) +
  freeze(white_noise_kernel(noise = 0.05), "noise")
kernel_tree(k)
}
