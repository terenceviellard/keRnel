% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stationary-kernels.R
\name{feature_kernel}
\alias{feature_kernel}
\title{Feature kernel}
\usage{
feature_kernel(
  length_scale,
  length_scale_u,
  variance,
  length_scale_parametrisation = log_exp_parametrisation(),
  length_scale_u_parametrisation = log_exp_parametrisation(),
  variance_parametrisation = log_exp_parametrisation(),
  distance_function = squared_euclidean_distance,
  engine = dense_engine()
)
}
\arguments{
\item{length_scale}{A strictly positive numeric value or length-2 vector.}

\item{length_scale_u}{A strictly positive numeric value.}

\item{variance}{A strictly positive numeric value or length-2 vector.}

\item{length_scale_parametrisation, length_scale_u_parametrisation, variance_parametrisation}{\link{parametrisation} objects. Default to \code{\link[=log_exp_parametrisation]{log_exp_parametrisation()}}.}

\item{distance_function}{A distance function, or a shortcut name (see
\code{\link[=resolve_distance_function]{resolve_distance_function()}}). Defaults to
\code{\link[=squared_euclidean_distance]{squared_euclidean_distance()}}.}

\item{engine}{A computation engine. Defaults to \code{\link[=dense_engine]{dense_engine()}}.}
}
\value{
An object of class \code{feature_kernel}/\code{stationary_kernel}/\code{kernel}.
}
\description{
A kernel modelling the covariance between two latent "feature" dimensions
observed with their own length scales, plus a shared length scale \code{u}:

\deqn{k(x_1, x_2) = \frac{\mathrm{variance}_1 \cdot \mathrm{variance}_2}
{(2\pi)^{D/2} \sqrt{\det \Sigma}}
\exp\left(-\frac{1}{2} \frac{d}{\Sigma}\right)}

where \eqn{\Sigma = \mathrm{length\_scale}_1 + \mathrm{length\_scale}_2 +
\mathrm{length\_scale\_u}}, \eqn{D} is the input dimensionality, and
\eqn{d} is \code{distance_function} (squared Euclidean by default).

\code{length_scale} and \code{variance} may each be given as a single value
(broadcast to both features) or a length-2 vector.
}
\examples{
k <- feature_kernel(length_scale = 1, length_scale_u = 1, variance = 1)
evaluate(k, c(0, 1, 2))
}
