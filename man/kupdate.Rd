% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kupdate.R
\name{kupdate}
\alias{kupdate}
\title{Immutably update a kernel's hyperparameters}
\usage{
kupdate(kernel, ...)
}
\arguments{
\item{kernel}{A kernel object.}

\item{...}{Named hyperparameter updates, e.g. \code{length_scale = 2}.}
}
\value{
A new kernel object with the requested updates applied.
}
\description{
Returns a copy of \code{kernel} with the named hyperparameter(s) updated to
new values, applied wherever they exist in the (possibly nested) kernel
tree. Validation of the new value happens inside \code{wrap()} of the relevant
\link{parametrisation} -- not duplicated here.

Named \code{kupdate()} rather than \code{replace()} because \code{base::replace()}
already exists in R and is not an S3 generic (it has no \code{UseMethod()}
call), so overriding it would mean shadowing a base function -- avoided
deliberately.

\code{kupdate()} is a single generic that works for every kernel, base or
composite, by walking \code{kernel$subkernels} (see \code{\link[=flatten_params]{flatten_params()}}),
rather than requiring each kernel type to implement its own update
logic.

If a named argument does not match \emph{any} hyperparameter, anywhere in the
kernel tree, \code{kupdate()} raises an error rather than silently ignoring it
(catches typos early). Likewise, a hyperparameter \link[=freeze]{frozen} in
\emph{every} occurrence where its name appears cannot be updated -- \code{kupdate()}
errors rather than silently no-op'ing (call \code{\link[=unfreeze]{unfreeze()}} first if that is
genuinely intended). If a name has a mix of frozen and non-frozen
occurrences in the tree (e.g. \code{\link[=ard_kernel]{ard_kernel()}}'s internal, deliberately
frozen \code{length_scale} alongside an unrelated, trainable \code{length_scale}
elsewhere in a larger composite kernel), only the non-frozen occurrences
are updated -- frozen ones are left untouched, never silently overwritten.
}
\examples{
k <- se_kernel(length_scale = 1)
k2 <- kupdate(k, length_scale = 5)
get_params(k2)
}
