% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/operators.R
\name{kernel_arithmetic}
\alias{kernel_arithmetic}
\alias{Ops.kernel}
\title{Arithmetic operators for kernels}
\usage{
\method{Ops}{kernel}(e1, e2)
}
\arguments{
\item{e1, e2}{Kernel objects, or numeric values.}
}
\value{
A new kernel object.
}
\description{
\code{k1 + k2} builds a \code{\link[=sum_kernel]{sum_kernel()}}; \code{k1 * k2} builds a \code{\link[=product_kernel]{product_kernel()}}.
Either side may be a bare number, automatically coerced to a
\code{\link[=constant_kernel]{constant_kernel()}} (see \code{\link[=as_kernel]{as_kernel()}}).

\code{-} (subtraction) and unary negation are deliberately not implemented --
there is no \code{neg_kernel()}. Subtracting one covariance kernel from
another rarely yields a valid (positive semi-definite) covariance
function, so it is omitted rather than silently producing an invalid
kernel.
}
\examples{
k <- se_kernel(length_scale = 1) + 0.5
evaluate(k, c(0, 1, 2))
}
