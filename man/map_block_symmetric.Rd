% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{map_block_symmetric}
\alias{map_block_symmetric}
\title{Build a symmetric block matrix, computing only the upper triangle of blocks}
\usage{
map_block_symmetric(
  kernel,
  X1,
  X2,
  nb_blocks,
  block_over_inputs,
  axes,
  self_covariance,
  ...
)
}
\arguments{
\item{kernel}{A kernel object (the \code{inner} of a \code{block_kernel}).}

\item{X1, X2}{Numeric matrices, or 3-dimensional arrays if
\code{block_over_inputs = TRUE}.}

\item{nb_blocks}{A positive integer.}

\item{block_over_inputs}{Logical.}

\item{...}{Passed on to \code{\link[=evaluate]{evaluate()}}.}
}
\value{
A numeric matrix of dimension \verb{(nrow(X1) * nb_blocks) x (nrow(X2) * nb_blocks)}
(or, with \code{block_over_inputs = TRUE}, \verb{(dim(X1)[2] * nb_blocks) x (dim(X2)[2] * nb_blocks)}).
}
\description{
The mapping primitive backing \code{\link[=block_kernel]{block_kernel()}}: computes block \verb{(i, j)}
for every \code{i <= j} via \code{\link[=pair_slice_kernel]{pair_slice_kernel()}} + \code{\link[=evaluate]{evaluate()}}, and fills
block \verb{(j, i)} as its transpose -- exploiting the symmetry of \code{kernel}'s
formula under swapping sides.
}
\keyword{internal}
