% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/freeze.R
\name{freeze}
\alias{freeze}
\alias{unfreeze}
\title{Freeze or unfreeze hyperparameters}
\usage{
freeze(kernel, ...)

unfreeze(kernel, ...)
}
\arguments{
\item{kernel}{A kernel object.}

\item{...}{Names of hyperparameters to freeze/unfreeze, e.g.
\code{"length_scale"}.}
}
\value{
A new kernel object with the requested \code{frozen} flags applied.
}
\description{
Marks named hyperparameters as excluded (\code{freeze()}) or included
(\code{unfreeze()}) from \code{\link[=get_trainable_params]{get_trainable_params()}}, wherever they exist in the
(possibly nested) kernel tree. The \code{frozen} flag lives directly on the
relevant \code{\link[=wrapped_param]{wrapped_param()}} object.

\code{frozen} requires no parallel structure to keep in sync: it is metadata
carried directly by the parameter itself.
}
\examples{
k <- se_kernel(length_scale = 1)
k_frozen <- freeze(k, "length_scale")
get_trainable_params(k_frozen)
}
