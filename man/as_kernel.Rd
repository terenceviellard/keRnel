% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/operators.R
\name{as_kernel}
\alias{as_kernel}
\title{Coerce a value to a kernel}
\usage{
as_kernel(x)
}
\arguments{
\item{x}{A kernel object, or a single finite numeric value.}
}
\value{
A kernel object.
}
\description{
Used by \code{\link[=sum_kernel]{sum_kernel()}}/\code{\link[=product_kernel]{product_kernel()}} (and the \code{+}/\code{*} operators), and
by every wrapper taking an \code{inner} kernel, to let a bare number stand in
for a constant kernel. Only a \code{kernel} object or a single finite numeric
value is accepted -- anything else (\code{NULL}, \code{NA}, a vector, a character
string...) is rejected here, at construction, rather than left to fail
later with a confusing low-level error once the malformed object reaches
\code{\link[=evaluate]{evaluate()}}.
}
\keyword{internal}
