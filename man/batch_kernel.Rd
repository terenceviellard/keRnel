% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{batch_kernel}
\alias{batch_kernel}
\title{Add batch handling to a kernel}
\usage{
batch_kernel(inner, batch_size, batch_over_inputs = FALSE, batch_axes = NULL)
}
\arguments{
\item{inner}{A kernel object, or a numeric value (coerced via
\code{\link[=as_kernel]{as_kernel()}}).}

\item{batch_size}{A positive integer (single-axis batching: the number
of batch elements), or a named integer vector with 2+ unique,
non-empty names (multi-axis batching: one size per named axis, e.g.
\code{c(K = 3, O = 2)}).}

\item{batch_over_inputs}{Logical, whether \code{x1}/\code{x2} are themselves
batched 3-dimensional arrays. Defaults to \code{FALSE}. In multi-axis mode,
the leading dimension of \code{x1}/\code{x2} must be \code{prod(batch_size)}, indexed
in the same column-major order as the output array.}

\item{batch_axes}{Single-axis mode: \code{NULL} (auto-detect, the default),
or a character vector naming the hyperparameters that vary along this
batch axis. Multi-axis mode: a required named list (hyperparameter
name -> character vector of axis names); auto-detection is not
supported.}
}
\value{
An object of class \code{batch_kernel}/\code{wrapper_kernel}/\code{kernel}.
}
\description{
Wraps \code{inner} so that \code{\link[=evaluate]{evaluate()}} produces \code{batch_size} covariance
matrices at once, one per batch element, stacked into an
\verb{nrow(x1) x nrow(x2) x batch_size} array.

Each batch element can use its own hyperparameter values: build \code{inner}
with a hyperparameter of length \code{batch_size} (one value per batch
element) rather than a single scalar, e.g. \code{se_kernel(length_scale = c(1, 2, 3))} for a batch of 3. A hyperparameter left as a single scalar
is shared across every batch element (see \code{\link[=slice_kernel]{slice_kernel()}}).

Which hyperparameters vary along the batch axis is an explicit contract
(see \code{\link[=.resolve_axes]{.resolve_axes()}}): by default (\code{batch_axes = NULL}) it is
auto-detected -- every sliceable multi-valued hyperparameter is assumed
to be batched, and must then have exactly \code{batch_size} values (any other
length is an error, not a silent recycling). Name the batched
hyperparameters explicitly (\code{batch_axes = "length_scale"}) whenever
auto-detection is ambiguous, most notably for \emph{nested} batching: a
hyperparameter not named in \code{batch_axes} is left untouched for another
(inner or outer) \code{batch_kernel()} to handle, which is what lets each
nesting level own its axis independently, with different sizes per
level. The resolved axes are validated again at every \code{\link[=evaluate]{evaluate()}} call,
so a \code{\link[=kupdate]{kupdate()}} that changes a value's length cannot silently
desynchronise the contract.

If \code{batch_over_inputs = TRUE}, \code{x1}/\code{x2} must themselves be 3-dimensional
arrays of shape \verb{(batch_size, n_points, n_dims)} -- a different set of
input points per batch element, regardless of whether hyperparameters
are batched. Defaults to \code{FALSE} (every batch element sees the same
\code{x1}/\code{x2}), the more common case for batched hyperparameters.

A \code{batch_kernel()} can itself be wrapped inside another \code{batch_kernel()},
to handle more than one batch dimension at once; declare each level's
\code{batch_axes} explicitly in that case. This works well when different
hyperparameters vary along different axes (e.g. \code{length_scale} along one
level, \code{noise} along another). When the \emph{same} hyperparameter must vary
along the full Cartesian product of two or more axes at once (e.g. a
\code{variance} that needs an independent value for every (cluster, output)
combination), nesting cannot express it -- each level's \code{\link[=slice_kernel]{slice_kernel()}}
collapses the hyperparameter to a scalar before the inner level ever sees
it. Use \strong{multi-axis batching} instead: pass a named integer vector as
\code{batch_size} (e.g. \code{c(K = 3, O = 2)}) and a named list as \code{batch_axes}
(hyperparameter name -> character vector of the axis names, a subset of
\code{names(batch_size)}, it varies along), all in a single \code{batch_kernel()}
call. Auto-detection is not available in this mode -- \code{batch_axes} must
be given explicitly, since a hyperparameter's length alone cannot
disambiguate which axis (or combination of axes) it belongs to. A
hyperparameter declared to vary along axes \code{c(a1, a2, ...)} must have
exactly \code{prod(batch_size[c(a1, a2, ...)])} values (or a single shared
one), stored in column-major order over those axes' sizes -- i.e. \code{a1}
is the fastest-varying axis, exactly the order \code{array(seq_len(prod(sizes)), dim = sizes)} would fill. A hyperparameter can also vary along only a
subset of the wrapper's axes (broadcasting across the others), or not be
named in \code{batch_axes} at all (fully shared).

Note: the result is an array of shape \verb{(n1, n2, batch_size)} (the batch
dimension(s) last, in \code{names(batch_size)} order for multi-axis) -- the
natural shape produced by \code{simplify2array()}, R's idiomatic way to stack
per-element matrices; use \code{aperm()} to reorder dimensions if a different
order is needed. Nested (single-axis) batching appends one more trailing
dimension per level: \verb{(n1, n2, inner_batch_size, outer_batch_size)};
multi-axis batching appends one dimension per named axis in a single
level: \verb{(n1, n2, batch_size["a1"], batch_size["a2"], ...)}.
}
\examples{
k <- batch_kernel(se_kernel(length_scale = c(1, 2, 3)), batch_size = 3)
evaluate(k, c(0, 1, 2)) # array of shape (3, 3, 3)

# Nested batching, one axis per level (declared explicitly):
k2 <- se_kernel(length_scale = c(.5, 1.5)) + white_noise_kernel(noise = c(0.1, 1, 2, 3))
bk <- batch_kernel(k2, batch_size = 2, batch_axes = "length_scale")
bbk <- batch_kernel(bk, batch_size = 4, batch_axes = "noise")
dim(evaluate(bbk, c(1, 2, 3))) # (3, 3, 2, 4)

# Multi-axis batching: `variance` varies over the full 3x2 = 6
# (cluster, output) grid in a single wrapper (column-major: cluster
# fastest-varying, so values 1:6 map to (K=1,O=1),(K=2,O=1),(K=3,O=1),
# (K=1,O=2),(K=2,O=2),(K=3,O=2)):
k3 <- variance_kernel(variance = 1:6) * se_kernel(length_scale = 1)
mk <- batch_kernel(k3, batch_size = c(K = 3, O = 2),
                    batch_axes = list(variance = c("K", "O")))
dim(evaluate(mk, c(0, 1, 2))) # (3, 3, 3, 2)
}
