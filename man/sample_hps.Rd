% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample-hps.R
\name{sample_hps}
\alias{sample_hps}
\title{Randomly sample a kernel's hyperparameters from a prior distribution}
\usage{
sample_hps(kernel, priors, distribution = stats::runif)
}
\arguments{
\item{kernel}{A kernel object.}

\item{priors}{A named list, each element a length-2 numeric vector:
\code{c(min, max)} for \code{distribution = stats::runif} (the default), or
\code{c(mean, sd)} for \code{distribution = stats::rnorm}.}

\item{distribution}{A sampling function with signature \verb{(n, p1, p2)},
e.g. \code{\link[stats:runif]{stats::runif()}} or \code{\link[stats:rnorm]{stats::rnorm()}}. Defaults to \code{\link[stats:runif]{stats::runif()}}.}
}
\value{
A new kernel object with the requested hyperparameters resampled.
}
\description{
Returns a copy of \code{kernel} with the named hyperparameter(s) resampled
from \code{priors}, in the kernel's natural (constrained) space, applied
wherever each hyperparameter exists in the (possibly nested) kernel tree
-- the same recursive walk as \code{\link[=kupdate]{kupdate()}} (via \code{\link[=flatten_params]{flatten_params()}}).

Frozen hyperparameters (see \code{\link[=freeze]{freeze()}}) are skipped, for consistency with
\code{\link[=get_trainable_params]{get_trainable_params()}}: resampling a value that will then be excluded
from optimisation is rarely useful, and risks silently overwriting a
value the caller deliberately fixed (e.g. \code{\link[=ard_kernel]{ard_kernel()}}'s inner
\code{length_scale}, always frozen at \code{1}).

\code{sample_hps()} relies on R's ambient RNG state -- call \code{\link[=set.seed]{set.seed()}}
beforehand for reproducibility, the standard convention for
\code{\link[stats:runif]{stats::runif()}}/\code{\link[stats:rnorm]{stats::rnorm()}} and the rest of R.

A typo in \code{priors}' names raises an error immediately, for consistency
with \code{\link[=kupdate]{kupdate()}}/\code{\link[=freeze]{freeze()}} elsewhere in this package.

A constraint violated by a sampled value (e.g. a negative draw for a
strictly positive \code{length_scale}) raises an error immediately, from
inside the relevant \link{parametrisation}'s \code{wrap()} -- the same single point
of validation used everywhere else in this package (no separate check
needed here).
}
\examples{
set.seed(1)
k <- se_kernel(length_scale = 1)

k2 <- sample_hps(k, priors = list(length_scale = c(0.1, 5)))
get_params(k2)

# Normal priors (mean, sd) instead of uniform (min, max):
k3 <- sample_hps(k, priors = list(length_scale = c(2, 0.5)), distribution = stats::rnorm)

# Frozen hyperparameters are skipped:
k4 <- freeze(k, "length_scale")
k5 <- sample_hps(k4, priors = list(length_scale = c(0.1, 5)))
identical(k4$length_scale, k5$length_scale)
}
