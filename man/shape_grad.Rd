% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernel-grad.R
\name{shape_grad}
\alias{shape_grad}
\title{Elementwise gradient of a stationary/dot-product kernel's \code{shape()}}
\usage{
shape_grad(kernel, d)
}
\arguments{
\item{kernel}{A kernel object.}

\item{d}{A numeric value, vector, or matrix of distances (as passed to
\code{\link[=shape]{shape()}}).}
}
\value{
A named list, one entry per own hyperparameter of \code{kernel}
(matching its field names), each the same shape as \code{d}.
}
\description{
The derivative counterpart of \code{\link[=shape]{shape()}}: for each of \code{kernel}'s own
hyperparameters, the partial derivative of \code{shape(kernel, d)} with respect
to that hyperparameter (in its natural, constrained space), evaluated at
\code{d}. Like \code{shape()}, this is a purely elementwise scalar transform, so it
vectorises over \code{d} being a value, vector, or matrix for free.

A \code{shape_grad()} method is what lets \code{\link[=pairwise_matrix_grad]{pairwise_matrix_grad()}}/
\code{\link[=pairwise_diag_grad]{pairwise_diag_grad()}} give a \code{stationary_kernel}/\code{dot_product_kernel} its
fast, vectorised gradient path -- the same role \code{shape()} plays for
\code{\link[=pairwise_matrix]{pairwise_matrix()}}/\code{\link[=pairwise_diag]{pairwise_diag()}}. A kernel with no \code{shape_grad()}
method is simply not covered by \code{\link[=kernel_grad]{kernel_grad()}} (see its documentation for
the exact list of kernels that are).
}
\examples{
k <- se_kernel(length_scale = 2)
shape_grad(k, c(0, 1, 4))
}
