% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drop-terms.R
\name{drop_terms}
\alias{drop_terms}
\title{Remove every matching sum term from a kernel tree}
\usage{
drop_terms(kernel, what)
}
\arguments{
\item{kernel}{A kernel object (any tree).}

\item{what}{A kernel S3 class name (e.g. \code{"white_noise_kernel"}), or a
predicate \verb{function(kernel) -> logical}.}
}
\value{
The rebuilt kernel, with the matched term(s) removed.
}
\description{
Walks \code{kernel} and rebuilds it with every sub-kernel matching \code{what}
removed. The semantics are deliberately strict rather than permissive:
\itemize{
\item In a sum, removing a term is the natural operation (\code{k1 + noise} ->
\code{k1}) -- this is the intended use case.
\item In a product, "removing" a factor would mean replacing it with the
identity (1) -- almost never what the caller actually wants (removing
a \code{variance_kernel()} factor doesn't "remove" anything, it rescales
the kernel). This is refused with an explicit error rather than
guessed.
\item If the match reaches the root itself, or the only remaining branch
of a wrapper, that is an error too: an empty kernel cannot exist.
}
}
\examples{
k <- se_kernel(length_scale = 1) + white_noise_kernel(noise = 0.1)
drop_terms(k, "white_noise_kernel")
}
