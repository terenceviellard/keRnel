% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{block_diag_kernel}
\alias{block_diag_kernel}
\title{Block-diagonal covariance matrix}
\usage{
block_diag_kernel(
  inner,
  nb_blocks,
  batch_over_inputs = FALSE,
  batch_axes = NULL
)
}
\arguments{
\item{inner}{A kernel object, or a numeric value (coerced via
\code{\link[=as_kernel]{as_kernel()}}).}

\item{nb_blocks}{A positive integer: the number of blocks.}

\item{batch_over_inputs}{Logical, whether \code{x1}/\code{x2} are themselves
per-block 3-dimensional arrays. Defaults to \code{FALSE}. See
\code{\link[=batch_kernel]{batch_kernel()}}.}

\item{batch_axes}{\code{NULL} (auto-detect, the default), or a character
vector naming the hyperparameters that vary per block. See
\code{\link[=batch_kernel]{batch_kernel()}}.}
}
\value{
An object of class \code{block_diag_kernel}/\code{wrapper_kernel}/\code{kernel}.
}
\description{
Wraps \code{inner} so that \code{\link[=evaluate]{evaluate()}} produces a block-diagonal matrix of
\code{nb_blocks} independent blocks (off-diagonal blocks are \code{0}) -- exactly
\code{\link[=batch_kernel]{batch_kernel()}}, with the resulting \verb{(n1, n2, nb_blocks)} array
reassembled into a single \verb{(n1 * nb_blocks) x (n2 * nb_blocks)} matrix.
Each block may have its own hyperparameter values the same way
\code{\link[=batch_kernel]{batch_kernel()}}'s do (see \code{\link[=slice_kernel]{slice_kernel()}}).
}
\examples{
k <- block_diag_kernel(se_kernel(length_scale = c(1, 2)), nb_blocks = 2)
evaluate(k, c(0, 1)) # 4x4, block-diagonal
}
