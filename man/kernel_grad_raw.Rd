% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernel-grad.R
\name{kernel_grad_raw}
\alias{kernel_grad_raw}
\title{Recursive tree-walk underneath \code{\link[=kernel_grad]{kernel_grad()}}}
\usage{
kernel_grad_raw(kernel, x1, x2)
}
\arguments{
\item{kernel}{A kernel object.}

\item{x1, x2}{As for \code{\link[=evaluate]{evaluate()}} (\code{x2 = NULL} means the auto-covariance
case).}
}
\value{
A named list (see \code{\link[=pairwise_matrix_grad]{pairwise_matrix_grad()}} for the shape of each
entry).
}
\description{
Like \code{\link[=flatten_params]{flatten_params()}} is to \code{\link[=get_params]{get_params()}}, \code{kernel_grad_raw()} is the
undisambiguated recursion underneath \code{\link[=kernel_grad]{kernel_grad()}}: one entry per
trainable hyperparameter, keyed by bare field name (duplicates allowed
when the same name occurs more than once in the tree), in the same
own-fields-then-subkernels order \code{\link[=.flatten_params_with_class]{.flatten_params_with_class()}} walks.
\code{\link[=kernel_grad]{kernel_grad()}} disambiguates the result once, at the very end -- doing
it here instead would disambiguate each subtree in isolation, missing
collisions with hyperparameters elsewhere in the tree.
}
\keyword{internal}
