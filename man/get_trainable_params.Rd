% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get-params.R
\name{get_trainable_params}
\alias{get_trainable_params}
\title{Get the non-frozen hyperparameters of a kernel}
\usage{
get_trainable_params(kernel)
}
\arguments{
\item{kernel}{A kernel object.}
}
\value{
A named numeric vector.
}
\description{
Same as \code{\link[=get_params]{get_params()}}, but excludes any \code{wrapped_param} marked
\code{frozen = TRUE} (via \code{\link[=freeze]{freeze()}}). This is the vector an optimiser should
actually act on. Names are disambiguated the same way as \code{\link[=get_params]{get_params()}}
when shared across sub-kernels.
}
\examples{
k <- freeze(se_kernel(length_scale = 2), "length_scale")
get_params(k)            # length_scale still listed
get_trainable_params(k)  # length_scale excluded
}
