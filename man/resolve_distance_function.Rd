% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distances.R
\name{resolve_distance_function}
\alias{resolve_distance_function}
\title{Resolve a distance function from a function or a shortcut name}
\usage{
resolve_distance_function(x)
}
\arguments{
\item{x}{A function, or a single string naming one of the distances in
\link{distances} (run \code{names(keRnel:::.distance_registry)} for the full
list).}
}
\value{
A distance function taking \verb{(x1, x2)}.
}
\description{
Lets kernel constructors accept either a distance function directly, or a
convenience string shortcut (e.g. \code{"euclidean"}), resolved against an
internal registry. Exported so a custom \code{stationary_kernel}/
\code{dot_product_kernel} constructor (see \code{vignette("b-intermediate")},
"Writing your own kernel") can support the same shortcut for its own
\code{distance_function} argument.

This shortcut is a deliberate ergonomic addition for R, mirroring
idioms such as \code{stats::dist(method = "euclidean")}.
}
\examples{
resolve_distance_function("manhattan")
}
