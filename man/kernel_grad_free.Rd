% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernel-grad.R
\name{kernel_grad_free}
\alias{kernel_grad_free}
\title{Analytic gradient of a kernel's covariance matrix, in free (unconstrained) space}
\usage{
kernel_grad_free(kernel, x1, x2 = NULL)
}
\arguments{
\item{kernel}{A kernel object (same support as \code{\link[=kernel_grad]{kernel_grad()}}).}

\item{x1, x2}{As for \code{\link[=kernel_grad]{kernel_grad()}}.}
}
\value{
An unnamed list, \code{length(get_free_params(kernel))} long, in the
same order. Each entry is the same shape as \code{evaluate(kernel, x1, x2)}.
}
\description{
Like \code{\link[=kernel_grad]{kernel_grad()}}, but chain-ruled through each hyperparameter's
\link{parametrisation} into the \emph{free} (wrapped, unconstrained) space
\code{\link[=get_free_params]{get_free_params()}}/\code{\link[=set_free_params]{set_free_params()}} work in, via \code{\link[=unwrap_grad]{unwrap_grad()}}
(\code{d(unwrap(p, w))/dw}).

The result is unnamed and strictly positional, in exactly the tree-walk
order \code{\link[=get_free_params]{get_free_params()}} uses -- including splitting a vector-valued
hyperparameter into one entry per component, the same way
\code{\link[=get_free_params]{get_free_params()}}'s \code{unlist()} does. This is what makes it safe to
zip element-by-element against \code{\link[=get_free_params]{get_free_params()}}'s own output, or to
hand a reduction of it straight to \code{\link[=set_free_params]{set_free_params()}}'s coordinate
system, \emph{even when two sub-kernels share a bare hyperparameter name} --
\code{\link[=kernel_grad]{kernel_grad()}}/\code{\link[=get_trainable_params]{get_trainable_params()}}'s names disambiguate that case
for display, but \code{\link[=kupdate]{kupdate()}} cannot address the two occurrences
independently by name; working positionally in free space sidesteps the
issue entirely, the same way \code{\link[=get_free_params]{get_free_params()}}/\code{\link[=set_free_params]{set_free_params()}}
already do for the \emph{values} themselves.

A worked example: an \code{optim(method = "L-BFGS-B")} M-step across a
Gaussian-process log-likelihood, without box constraints (positivity is
already guaranteed by each hyperparameter's parametrisation) --

\if{html}{\out{<div class="sourceCode r">}}\preformatted{gr <- function(theta, kernel, X, alpha, K_inv) \{
  k <- set_free_params(kernel, theta)
  grads <- kernel_grad_free(k, X)
  vapply(grads, function(dK) \{
    -0.5 * sum(diag((alpha \%*\% t(alpha) - K_inv) \%*\% dK))
  \}, numeric(1))
\}
optim(get_free_params(kernel), fn, gr = gr, method = "L-BFGS-B", kernel = kernel, X = X, ...)
}\if{html}{\out{</div>}}
}
\examples{
k <- se_kernel(length_scale = 2)
x <- c(0, 1, 3)
kernel_grad_free(k, x)[[1]]

# Insensitive to a bare-name collision that would make kupdate() ambiguous:
k2 <- se_kernel(length_scale = 1) + se_kernel(length_scale = 3)
length(kernel_grad_free(k2, x)) == length(get_free_params(k2))
}
