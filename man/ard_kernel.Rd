% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{ard_kernel}
\alias{ard_kernel}
\title{Automatic Relevance Determination (ARD)}
\usage{
ard_kernel(
  constructor,
  length_scales,
  ...,
  active = "length_scale",
  length_scales_parametrisation = log_exp_parametrisation()
)
}
\arguments{
\item{constructor}{A kernel constructor function, e.g. \code{\link[=se_kernel]{se_kernel()}},
taking a \code{length_scale} argument (or whichever argument name \code{active}
specifies).}

\item{length_scales}{A vector of strictly positive numeric values, one
per input dimension.}

\item{...}{Additional arguments passed to \code{constructor} (e.g. \code{period}
for \code{\link[=periodic_kernel]{periodic_kernel()}}).}

\item{active}{The name of \code{constructor}'s length-scale argument. Defaults
to \code{"length_scale"}.}

\item{length_scales_parametrisation}{A \link{parametrisation} for
\code{length_scales}. Defaults to \code{\link[=log_exp_parametrisation]{log_exp_parametrisation()}}.}
}
\value{
An object of class \code{ard_kernel}/\code{wrapper_kernel}/\code{kernel}.
}
\description{
Wraps a stationary kernel constructor to give every input dimension its
own \code{length_scale}:

\deqn{k(x_1, x_2) = k_{\mathrm{inner}}(x_1 / \mathrm{length\_scales}, x_2 / \mathrm{length\_scales})}

\code{ard_kernel()} takes a kernel \strong{constructor} (a function), not a
pre-built instance. It builds the inner kernel itself with
\code{length_scale = 1} and immediately \code{\link[=freeze]{freeze()}}s it -- the contract that
every per-dimension \code{length_scale} must start at \code{1} and stay frozen
cannot be violated by the caller, because the caller never gets to build
(or therefore mis-build) the inner kernel.

(Residual risk, accepted and documented: \code{\link[=kupdate]{kupdate()}} does not itself
check \code{frozen} before overwriting a value, so
\code{kupdate(ard, length_scale = 5)} would still bypass this protection if
called directly on the inner kernel's hyperparameter name -- a
deliberate, explicitly documented limitation rather than an oversight.)
}
\examples{
k <- ard_kernel(se_kernel, length_scales = c(1, 2, 3))
X <- matrix(rnorm(9), ncol = 3)
evaluate(k, X)

# Additional constructor arguments are passed through `...`:
k2 <- ard_kernel(periodic_kernel, length_scales = c(1, 2), period = 5)
}
