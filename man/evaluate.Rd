% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate.R
\name{evaluate}
\alias{evaluate}
\title{Evaluate a kernel's covariance matrix}
\usage{
evaluate(
  kernel,
  x1,
  x2 = NULL,
  na_action = c("propagate", "mask"),
  mask_variance = 1,
  memory_warning_threshold = 5000,
  ...
)
}
\arguments{
\item{kernel}{A kernel object.}

\item{x1}{A numeric vector or matrix of input points (see
\code{\link[=as_matrix_input]{as_matrix_input()}}).}

\item{x2}{An optional second set of input points; defaults to \code{x1}
(the auto-covariance case).}

\item{na_action}{One of \code{"propagate"} (the default) or \code{"mask"}.}

\item{mask_variance}{A single non-negative numeric value, used when
\code{na_action = "mask"}. Defaults to \code{1}.}

\item{memory_warning_threshold}{A single numeric value: \code{evaluate()} warns
if \code{nrow(x1)} or \code{nrow(x2)} exceeds it. Defaults to \code{5000}; set to \code{Inf}
to disable.}

\item{...}{Passed on to methods (and, for composite kernels, forwarded to
the \code{evaluate()} call of each sub-kernel). One internal argument
travels this way: \code{.self_covariance}, the \emph{self-covariance contract}.
Whether the requested matrix is a self-covariance (\code{x2} missing or
equal to \code{x1}) can only be decided at the level of the original call:
wrappers that re-\code{evaluate()} their sub-kernel on transformed inputs
(a single row of \code{x1}, one block's slice...) would mis-derive it
locally, since a transformed \code{x1} no longer compares equal to \code{x2}.
Such wrappers therefore decide it \emph{once}, before transforming
anything, and forward \code{.self_covariance} explicitly; the default
method trusts a forwarded value over re-deriving its own. User code
should never need to pass it.}
}
\value{
A numeric matrix of dimension \code{nrow(x1)} x \code{nrow(x2)} (or, with
\code{\link[=diagonal_engine]{diagonal_engine()}}, a numeric vector of length \code{nrow(x1)}).
}
\description{
The top-level, user-facing entry point. Dispatches on the \emph{class of the
kernel} (layer 4 of the architecture, see \code{dev/ameliorations/design/01-architecture.Rmd}):
\itemize{
\item The default method handles every base kernel: it coerces \code{x1}/\code{x2} to
matrices, handles missing values, and delegates to \code{\link[=engine_eval]{engine_eval()}}
(which in turn delegates to a mapping primitive and \code{\link[=pairwise]{pairwise()}}).
\item Composite/wrapper kernels (sums, products, exponentials, logarithms...)
override \code{evaluate()} directly, bypassing \code{engine_eval()}/\code{\link[=pairwise]{pairwise()}}
entirely for themselves, and instead recombine the \code{evaluate()} results
of their sub-kernels, forwarding \code{na_action}/\code{mask_variance} down via
\code{...} so missing-value handling stays consistent however deeply a
kernel is composed.
}

Missing values (\code{NA}) in \code{x1}/\code{x2} are handled according to \code{na_action}:
\itemize{
\item \code{"propagate"} (the default): \code{NA} in the input produces \code{NA} in the
corresponding row/column of the result. The most predictable behaviour,
but a covariance matrix containing \code{NA} cannot be inverted -- the
caller must handle missing data explicitly before, e.g., a Cholesky
factorisation.
\item \code{"mask"}: a point with a missing value is treated as an independent
observation of variance \code{mask_variance} (\code{1} by default), statistically
neutral (adds no information about, and is not informed by, any other
point) but keeps the resulting matrix positive-definite and therefore
invertible. This is formalised here as an explicit \code{evaluate()} option
rather than a separate engine, since engines should only encode \emph{how} a
result is vectorised, not domain-specific missing-data policy.
}

With a kernel using \code{\link[=diagonal_engine]{diagonal_engine()}}, the result is a vector (not a
matrix): \code{"propagate"} sets \code{NA} at index \code{i} whenever the aligned pair
\code{x1[i, ]}/\code{x2[i, ]} has a missing value; \code{"mask"} sets \code{mask_variance}
there instead.

\code{evaluate()} also warns (not errors) if the matrix it is about to build
is large, since a dense \verb{n1 x n2} matrix can consume significant memory
and compute time. Raise or lower \code{memory_warning_threshold}, or set it to
\code{Inf}, to adjust; this is a safety net, not a hard limit.

A base kernel with a multi-valued hyperparameter (e.g.
\code{se_kernel(length_scale = c(1, 2, 3))}) errors if evaluated directly: such
a value is only meaningful sliced down by \code{\link[=batch_kernel]{batch_kernel()}}/
\code{\link[=block_kernel]{block_kernel()}}/\code{\link[=input_specific_param_kernel]{input_specific_param_kernel()}}, which do so before
delegating to \code{evaluate()}. Evaluated directly, the vector would
otherwise be silently recycled across the covariance matrix by R's usual
recycling rules, producing a mathematically meaningless -- often
non-symmetric -- result with no warning at all.
}
\examples{
k <- se_kernel(length_scale = 1)
evaluate(k, c(0, 1, 2))
evaluate(k, c(0, 1), c(0, 10))

# Keep the covariance matrix invertible despite missing data:
evaluate(k, c(0, NA, 2), na_action = "mask")
}
