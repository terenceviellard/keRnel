% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernel-grad.R
\name{kernel_grad}
\alias{kernel_grad}
\title{Analytic gradient of a kernel's covariance matrix}
\usage{
kernel_grad(kernel, x1, x2 = NULL)
}
\arguments{
\item{kernel}{A kernel object (see the "Supported kernels" list above).}

\item{x1}{A numeric vector or matrix of input points (see
\code{\link[=as_matrix_input]{as_matrix_input()}}).}

\item{x2}{An optional second set of input points; defaults to \code{x1}.}
}
\value{
A named list, one entry per non-frozen hyperparameter -- same
names as \code{\link[=get_trainable_params]{get_trainable_params()}} (disambiguated the same way when a
bare name collides elsewhere in the tree). Each entry has the shape
described in \code{\link[=pairwise_matrix_grad]{pairwise_matrix_grad()}}.
}
\description{
For each non-frozen hyperparameter of \code{kernel} (in its natural,
constrained space -- the same space \code{\link[=get_trainable_params]{get_trainable_params()}} reports),
the partial derivative of \code{evaluate(kernel, x1, x2)} with respect to that
hyperparameter. Handing this to an optimiser (e.g. \code{optim(method = "L-BFGS-B", gr = ...)}) alongside the usual objective avoids the \verb{2p + 1}
finite-difference evaluations per step a gradient-free optimiser needs
(see \code{dev/ameliorations/README.md}, backlog item 2b) -- one analytic pass
instead.

\code{kernel_grad()} does not compute a likelihood gradient itself: like the
rest of keRnel, it stays a general-purpose kernel package (see
\code{dev/ameliorations/README.md}, section D) and leaves the reduction (e.g.
the Gaussian-process marginal-likelihood trace formula) to the caller,
who combines each returned matrix with \code{K^{-1}} and the residuals however
their model requires.

Supported on the base kernels -- \code{\link[=se_kernel]{se_kernel()}}, \code{\link[=matern12_kernel]{matern12_kernel()}}/
\code{\link[=matern32_kernel]{matern32_kernel()}}/\code{\link[=matern52_kernel]{matern52_kernel()}}, \code{\link[=periodic_kernel]{periodic_kernel()}},
\code{\link[=rational_quadratic_kernel]{rational_quadratic_kernel()}}, \code{\link[=white_noise_kernel]{white_noise_kernel()}}, \code{\link[=feature_kernel]{feature_kernel()}},
\code{\link[=linear_kernel]{linear_kernel()}}, \code{\link[=affine_kernel]{affine_kernel()}}, \code{\link[=polynomial_kernel]{polynomial_kernel()}},
\code{\link[=sigmoid_kernel]{sigmoid_kernel()}}, \code{\link[=constant_kernel]{constant_kernel()}}, \code{\link[=variance_kernel]{variance_kernel()}} -- any
\code{\link[=sum_kernel]{sum_kernel()}}/\code{\link[=product_kernel]{product_kernel()}} composition of them (including the
compositions \code{\link[=kernel_interactions]{kernel_interactions()}} builds); \code{\link[=exp_kernel]{exp_kernel()}} and
\code{\link[=log_kernel]{log_kernel()}}; \code{\link[=active_dims_kernel]{active_dims_kernel()}}; and \code{\link[=ard_kernel]{ard_kernel()}}, provided its
wrapped kernel has a \code{\link[=shape]{shape()}} method and one of \code{squared_euclidean_distance()}
/\code{euclidean_distance()}/\code{manhattan_distance()}/\code{dot_product()} as its
\code{distance_function} (a clear error names the unsupported kernel/distance
otherwise).

Not supported on the remaining wrapper kernels -- \code{\link[=batch_kernel]{batch_kernel()}},
\code{\link[=block_kernel]{block_kernel()}}, \code{\link[=block_diag_kernel]{block_diag_kernel()}}, \code{\link[=input_specific_param_kernel]{input_specific_param_kernel()}}
-- which slice hyperparameters per batch/block/input element rather than
compute a single \code{shape()}/\code{pairwise_matrix()} formula. Calling
\code{kernel_grad()} on one of these, or on a composition that contains one,
raises a clear error naming the unsupported class.

Unlike \code{\link[=evaluate]{evaluate()}}, \code{kernel_grad()} has no \code{na_action}: \code{x1}/\code{x2} must
be free of missing/non-finite values.
}
\examples{
k <- se_kernel(length_scale = 2)
x <- c(0, 1, 3)
g <- kernel_grad(k, x)
g$length_scale

# A sum_kernel()/product_kernel() composition works the same way:
k2 <- se_kernel(length_scale = 2) + white_noise_kernel(noise = 0.1)
names(kernel_grad(k2, x))
}
