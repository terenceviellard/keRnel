% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kupdate.R
\name{set_free_params}
\alias{set_free_params}
\title{Set a kernel's free hyperparameters from the unconstrained (wrapped) space}
\usage{
set_free_params(kernel, values)
}
\arguments{
\item{kernel}{A kernel object.}

\item{values}{A finite numeric vector, \code{length(get_free_params(kernel))}
long.}
}
\value{
A new kernel object with the free hyperparameters updated.
}
\description{
The write-side counterpart of \code{\link[=get_free_params]{get_free_params()}}: assigns \code{values}
positionally, in the same tree-walk order \code{\link[=get_free_params]{get_free_params()}} reads in,
directly to each non-frozen hyperparameter's \verb{$wrapped} field -- no
\code{\link[=wrap]{wrap()}} is applied, so \code{set_free_params(k, get_free_params(k))}
round-trips bit-exactly back to \code{k}. \code{values} must have exactly
\code{\link[=get_free_params]{get_free_params()}}'s length; a multi-valued hyperparameter (e.g.
\code{\link[=ard_kernel]{ard_kernel()}}'s \code{length_scales}) consumes as many consecutive elements
as it has values.
}
\section{Numerical safety}{

Only finiteness is checked here -- there is no upper bound on \code{values},
by design (the unconstrained space is, in general, unbounded). This
matters for an optimiser driving \code{values} directly (e.g. \code{optim()} over
\code{\link[=get_free_params]{get_free_params()}}'s output): a single overly large step can produce a
wrapped value whose unwrapped (constrained) counterpart silently
overflows to an astronomically large but still-finite number -- e.g. a
\code{\link[=log_exp_parametrisation]{log_exp_parametrisation()}} value of \code{700} unwraps to \code{exp(700) ~ 1e304},
close to \code{double}'s range limit but not \code{Inf} -- which \code{\link[=evaluate]{evaluate()}} and
\code{\link[=robust_chol]{robust_chol()}} will happily compute with, no warning, no error. Only a
large enough step to actually reach \code{Inf}/\code{NaN} is caught (downstream, by
\code{\link[=chol]{chol()}}/\code{\link[=evaluate]{evaluate()}} themselves, not here). If driving \code{values} with an
unbounded optimiser, consider a method that accepts box constraints
(e.g. \code{optim(..., method = "L-BFGS-B", lower = , upper = )}) to keep
steps within a sane range for your problem.
}

\examples{
k <- se_kernel(length_scale = 1)
free <- get_free_params(k)
k2 <- set_free_params(k, free + 1) # a step in unconstrained space
get_trainable_params(k2)
}
