% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{.resolve_axes_multi}
\alias{.resolve_axes_multi}
\title{Resolve which hyperparameters vary along which axis of a multi-axis batch wrapper}
\usage{
.resolve_axes_multi(inner, batch_size, batch_axes, axes_arg, size_arg)
}
\arguments{
\item{inner}{A kernel object (the wrapper's sub-kernel).}

\item{batch_size}{A named integer vector (axis name -> axis size).}

\item{batch_axes}{A named list (hyperparameter name -> character vector
of axis names).}

\item{axes_arg, size_arg}{The wrapper's argument names, for error messages.}
}
\value{
\code{batch_axes}, unchanged, if every declaration is valid.
}
\description{
The multi-axis counterpart of \code{\link[=.resolve_axes]{.resolve_axes()}}. \code{batch_axes} must be an
explicit named list (hyperparameter name -> character vector of axis
names, a subset of \code{names(batch_size)}) -- auto-detection is not
supported, since a hyperparameter's length alone cannot disambiguate
which axis, or which combination of axes, it belongs to once more than
one axis is in play. Re-validated at every \code{evaluate()} call, exactly
like \code{\link[=.resolve_axes]{.resolve_axes()}}.
}
\keyword{internal}
