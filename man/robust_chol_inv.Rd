% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linalg.R
\name{robust_chol_inv}
\alias{robust_chol_inv}
\title{Robust matrix inverse via Cholesky, dimnames preserved}
\usage{
robust_chol_inv(mat, pen_diag = 1e-10)
}
\arguments{
\item{mat}{A symmetric (quasi-)positive-definite numeric matrix.}

\item{pen_diag}{Initial jitter added to the diagonal; multiplied by 10 on
each \code{chol()} failure.}
}
\value{
The inverse of \code{mat + jitter} (see \code{\link[=robust_chol]{robust_chol()}}), with the
same \code{dimnames()} as \code{mat}.
}
\description{
Robust matrix inverse via Cholesky, dimnames preserved
}
\examples{
k <- se_kernel(length_scale = 1)
K <- evaluate(k, c(0, 1, 2))
robust_chol_inv(K)
}
