% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parametrisations.R
\name{.validate_finite_numeric}
\alias{.validate_finite_numeric}
\title{Reject non-numeric/non-finite input before any parametrisation transform}
\usage{
.validate_finite_numeric(x, parametrisation_name)
}
\arguments{
\item{x}{The value passed to \code{wrap()}.}

\item{parametrisation_name}{The parametrisation's class name, for the
error message.}
}
\description{
Shared first check for every \verb{wrap.*} method, called before any
constraint-specific logic (e.g. positivity). Without it, a non-numeric or
non-finite value either slips through silently (\code{Inf <= 0} is \code{FALSE}, so
a positivity check alone lets \code{Inf} through) or fails deep inside a
comparison/transform with a confusing low-level error (\verb{missing value where TRUE/FALSE needed}, \verb{non-numeric argument to mathematical function}) instead of a clear one naming the parametrisation.
}
\keyword{internal}
