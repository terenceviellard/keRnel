% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-kernel-contract.R
\name{check_kernel_contract}
\alias{check_kernel_contract}
\title{Run a battery of runtime sanity checks on a kernel}
\usage{
check_kernel_contract(kernel, X = NULL, n_points = 5, tol = 1e-06)
}
\arguments{
\item{kernel}{A kernel object.}

\item{X}{Points to evaluate at. Defaults to \code{n_points} random 1-D points.}

\item{n_points}{Number of random points to use if \code{X} is omitted.
Defaults to \code{5}.}

\item{tol}{Numerical tolerance. Defaults to \code{1e-6}.}
}
\value{
An object of class \code{kernel_contract_check} (a list of named
results), printed as a short pass/fail summary.
}
\description{
A heavier, opt-in complement to \code{\link[=validate_kernel]{validate_kernel()}} (which only checks
structure, at construction time, and must stay cheap). Meant to be run by
hand -- typically once, while writing a custom kernel (see
\code{vignette("b-intermediate")}, "Writing your own kernel") -- to catch
mistakes \code{validate_kernel()} cannot: a formula that isn't symmetric, a
covariance matrix that isn't positive semi-definite, an \code{evaluate()}
method that mishandles the self-covariance case, or a hyperparameter name
accidentally shared with a sibling sub-kernel.

Checks performed, evaluated on a small set of points (\code{X}, or \code{n_points}
random ones if \code{X} is omitted):
\itemize{
\item \code{symmetric}: \code{evaluate(kernel, X)} is symmetric.
\item \code{positive_semidefinite}: its eigenvalues are \verb{>= -tol} (relative to
their own scale).
\item \code{self_covariance_consistent}: \code{evaluate(kernel, X)} equals
\code{evaluate(kernel, X, X)} (the self-covariance and explicit-\code{x2} code
paths must agree).
\item \code{kupdate_roundtrip}: reapplying each trainable hyperparameter's current
value via \code{\link[=kupdate]{kupdate()}} reproduces the same result -- \code{NA} if no
hyperparameter has a single, unambiguous current value to round-trip
(i.e. every trainable name is either unique or already shares an equal
value at every occurrence; see \code{shared_param_names}).
\item \code{shared_param_names}: the result of \code{\link[=check_param_names]{check_param_names()}}, for
information -- not itself a pass/fail check (a shared name is often
intentional).
}

A kernel using an engine other than the default dense one (e.g.
\code{\link[=diagonal_engine]{diagonal_engine()}}, whose \code{evaluate()} returns a vector, not a matrix)
skips \code{symmetric}/\code{positive_semidefinite} (\code{NA}): those checks assume a
full covariance matrix.
}
\examples{
check_kernel_contract(se_kernel(length_scale = 1))
}
