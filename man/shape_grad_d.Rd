% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernel-grad.R
\name{shape_grad_d}
\alias{shape_grad_d}
\title{Elementwise derivative of \code{shape()} with respect to the distance itself}
\usage{
shape_grad_d(kernel, d)
}
\arguments{
\item{kernel}{A kernel object.}

\item{d}{A numeric value, vector, or matrix of distances (as passed to
\code{\link[=shape]{shape()}}).}
}
\value{
The same shape as \code{d}.
}
\description{
Unlike \code{\link[=shape_grad]{shape_grad()}} (the derivative with respect to one of \code{kernel}'s
own hyperparameters), \code{shape_grad_d()} differentiates \code{shape(kernel, d)}
with respect to \code{d} itself. This is the piece \code{\link[=ard_kernel]{ard_kernel()}} needs: it
feeds its inner kernel a \emph{transformed} distance (computed on \code{x / length_scales}), so getting from a \code{length_scales} gradient back to a
hyperparameter gradient means chain-ruling through \code{shape()} at the
distance level, not the hyperparameter level.
}
\examples{
k <- se_kernel(length_scale = 2)
shape_grad_d(k, c(0, 1, 4))
}
