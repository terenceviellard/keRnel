% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernel-grad.R
\name{pairwise_grad}
\alias{pairwise_grad}
\alias{pairwise_matrix_grad}
\alias{pairwise_diag_grad}
\title{Gradient of the vectorised pairwise covariance matrix/diagonal}
\usage{
pairwise_matrix_grad(kernel, X1, X2)

pairwise_diag_grad(kernel, X1, X2)
}
\arguments{
\item{kernel}{A kernel object.}

\item{X1, X2}{Numeric matrices.}
}
\value{
A named list, one entry per own hyperparameter of \code{kernel}. A
scalar-valued hyperparameter's entry is a numeric matrix (dimension
\code{nrow(X1)} x \code{nrow(X2)}, for \code{pairwise_matrix_grad()}) or vector (length
\code{nrow(X1)}, for \code{pairwise_diag_grad()}); a vector-valued hyperparameter
(currently only \code{feature_kernel()}'s \code{length_scale}/\code{variance}) gets a
\emph{list} of one such matrix/vector per component instead, in the same
order as the hyperparameter's own value.
}
\description{
The derivative counterparts of \code{\link[=pairwise_matrix]{pairwise_matrix()}}/\code{\link[=pairwise_diag]{pairwise_diag()}}:
for each of \code{kernel}'s own hyperparameters, the partial derivative of the
covariance matrix (or, for \code{pairwise_diag_grad()}, its diagonal) with
respect to that hyperparameter. \code{stationary_kernel}/\code{dot_product_kernel}
subclasses get a fast method via \code{\link[=distance_matrix]{distance_matrix()}}/\code{\link[=diag_distance]{diag_distance()}} +
\code{\link[=shape_grad]{shape_grad()}}, the same pattern \code{\link[=pairwise_matrix]{pairwise_matrix()}}/\code{\link[=pairwise_diag]{pairwise_diag()}}
use with \code{\link[=shape]{shape()}}. A handful of kernels without a \code{shape()} method
(\code{constant_kernel()}, \code{variance_kernel()}, \code{feature_kernel()}) get a
direct method instead.
}
\keyword{internal}
