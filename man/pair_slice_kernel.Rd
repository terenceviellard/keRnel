% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{pair_slice_kernel}
\alias{pair_slice_kernel}
\title{Extract a pair of batch/block elements of a kernel's hyperparameters}
\usage{
pair_slice_kernel(kernel, i, j, axes)
}
\arguments{
\item{kernel}{A kernel object.}

\item{i, j}{Positive integer indices.}

\item{axes}{A character vector of hyperparameter names (the resolved
block axes).}
}
\value{
A new kernel object, with every axis hyperparameter replaced by
its \code{c(i, j)} pair.
}
\description{
Like \code{\link[=slice_kernel]{slice_kernel()}}, but replaces every \code{wrapped_param} named in
\code{axes} (with more than one value) with the \strong{pair} of its \code{i}-th and
\code{j}-th elements (a length-2 vector), rather than a single element. Used
by \code{\link[=block_kernel]{block_kernel()}} to build the cross-block (block \code{i}, block \code{j})
term, which needs both blocks' hyperparameter values available at once
(e.g. \code{\link[=feature_kernel]{feature_kernel()}}'s paired \code{length_scale}/\code{variance}).
}
\keyword{internal}
