% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-kernel-contract.R
\name{check_param_names}
\alias{check_param_names}
\title{Detect hyperparameter names shared across sibling sub-kernels}
\usage{
check_param_names(kernel)
}
\arguments{
\item{kernel}{A kernel object.}
}
\value{
A character vector of hyperparameter names that appear more than
once in \code{kernel}'s tree (empty if none).
}
\description{
\code{\link[=kupdate]{kupdate()}}/\code{\link[=freeze]{freeze()}} intentionally propagate a named update to \emph{every}
occurrence of that name anywhere in the kernel tree -- documented, tested
behaviour (see \code{\link[=kupdate]{kupdate()}}), not a bug. Sharing a hyperparameter name
across sub-kernels is often deliberate: \code{\link[=kernel_interactions]{kernel_interactions()}}, for
instance, routinely combines kernels from different families that happen
to both use \code{length_scale}.

This function does not error or warn on its own -- it is a diagnostic to
call when you \emph{suspect} an accidental collision (e.g. after a typo in a
hand-built composite kernel), not a validation gate on every
construction. \code{\link[=get_params]{get_params()}}/\code{\link[=get_trainable_params]{get_trainable_params()}} already return
safely disambiguated names regardless of whether you check this first.
}
\examples{
k <- se_kernel(length_scale = 1) + periodic_kernel(length_scale = 2, period = 3)
check_param_names(k) # "length_scale"

k2 <- se_kernel(length_scale = 1) + linear_kernel(slope_var = 1)
check_param_names(k2) # character(0): no shared name
}
