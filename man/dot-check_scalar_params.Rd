% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate.R
\name{.check_scalar_params}
\alias{.check_scalar_params}
\title{A hyperparameter with more than one value only has a well-defined meaning
when a batching/blocking wrapper mediates the evaluation -- \code{slice_kernel()}/
\code{slice_kernel_nd()}/\code{pair_slice_kernel()} (\code{R/wrappers.R}) always reduce it
to a single value (or a pair, for a \link[=block_kernel]{block_compatible}
kernel) \emph{before} the sliced kernel ever reaches \code{evaluate.default()}.
Reaching this function with a longer vector therefore means the kernel is
being evaluated directly, without such a wrapper: R would silently recycle
the vector across the covariance matrix (e.g. row-by-row, wrapping around),
producing a mathematically meaningless -- often non-symmetric -- result
with no error at all. Caught here, once, for every base kernel, rather
than duplicated in each \code{shape()}/\code{pairwise()} method.}
\usage{
.check_scalar_params(kernel)
}
\description{
A hyperparameter with more than one value only has a well-defined meaning
when a batching/blocking wrapper mediates the evaluation -- \code{slice_kernel()}/
\code{slice_kernel_nd()}/\code{pair_slice_kernel()} (\code{R/wrappers.R}) always reduce it
to a single value (or a pair, for a \link[=block_kernel]{block_compatible}
kernel) \emph{before} the sliced kernel ever reaches \code{evaluate.default()}.
Reaching this function with a longer vector therefore means the kernel is
being evaluated directly, without such a wrapper: R would silently recycle
the vector across the covariance matrix (e.g. row-by-row, wrapping around),
producing a mathematically meaningless -- often non-symmetric -- result
with no error at all. Caught here, once, for every base kernel, rather
than duplicated in each \code{shape()}/\code{pairwise()} method.
}
\keyword{internal}
