% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate.R
\name{evaluate_named}
\alias{evaluate_named}
\title{evaluate() with point identity propagated as dimnames}
\usage{
evaluate_named(kernel, x1, x2 = NULL, ...)
}
\arguments{
\item{kernel}{A kernel object.}

\item{x1}{Inputs, as for \code{\link[=evaluate]{evaluate()}}; a matrix/data frame's \code{rownames()}
or a bare vector's \code{names()} trigger the propagation.}

\item{x2}{Optional second set of inputs, as for \code{\link[=evaluate]{evaluate()}}.}

\item{...}{Forwarded to \code{\link[=evaluate]{evaluate()}}.}
}
\value{
The result of \code{\link[=evaluate]{evaluate()}}, with dimnames attached when available.
}
\description{
\code{evaluate()} itself never looks at \code{rownames(x1)}/\code{names(x1)} (a bare
matrix in, a bare matrix out) -- propagating point identity onto the
result is left to the caller. \code{evaluate_named()} is that propagation,
wrapped once: if \code{x1}/\code{x2} carry names (\code{rownames()} for a matrix or
data frame, \code{names()} for a bare vector), they become the result's
\code{rownames()}/\code{colnames()} (or, for a \code{\link[=batch_kernel]{batch_kernel()}} array, the first
two dimnames; for a \code{\link[=diagonal_engine]{diagonal_engine()}} vector, its \code{names()}). With no
names on either side, the result is returned unchanged.
}
\examples{
x <- c(a = 0, b = 1, c = 2)
evaluate_named(se_kernel(length_scale = 1), x)
}
