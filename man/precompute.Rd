% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan.R
\name{precompute}
\alias{precompute}
\title{Precompute a distance-based evaluation plan for a kernel tree}
\usage{
precompute(kernel, x1, x2 = NULL)
}
\arguments{
\item{kernel}{A kernel object.}

\item{x1, x2}{Inputs, as for \code{\link[=evaluate]{evaluate()}}.}
}
\value{
An object of class \code{kernel_plan}.
}
\description{
Builds a plan for \code{kernel}: a tree parallel to its structure where every
leaf whose distance matrix does not depend on its hyperparameters (any
stationary or dot-product kernel with a \code{\link[=shape]{shape()}} method, except
\code{\link[=affine_kernel]{affine_kernel()}} whose distance depends on \code{offset}) has that distance
precomputed once. \code{\link[=eval_plan]{eval_plan()}} then re-evaluates the covariance for any
kernel sharing this exact tree structure (typically the same kernel after
\code{\link[=kupdate]{kupdate()}}, which never changes structure) by applying only \code{\link[=shape]{shape()}} to
the cached distance -- skipping the distance recomputation entirely.

Any node the plan cannot cache (\code{block_kernel},
\code{input_specific_param_kernel}, \code{affine_kernel}, a custom kernel without a
\code{shape()} method, ...) is recorded as a fallback leaf: \code{\link[=eval_plan]{eval_plan()}} calls
\code{\link[=evaluate]{evaluate()}} on that subtree directly, exactly as it would without a
plan. Missing/infinite values in \code{x1}/\code{x2} are detected once here (same
point-level rule as \code{\link[=evaluate]{evaluate()}}) and applied by \code{\link[=eval_plan]{eval_plan()}} per leaf,
reproducing \code{\link[=evaluate]{evaluate()}}'s existing \code{na_action} semantics exactly.
}
\examples{
k <- se_kernel(length_scale = 1)
plan <- precompute(k, c(0, 1, 2))
eval_plan(plan, se_kernel(length_scale = 2))
}
\seealso{
\code{\link[=eval_plan]{eval_plan()}}
}
