% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get-params.R
\name{get_free_params}
\alias{get_free_params}
\title{Get a kernel's free hyperparameters in the unconstrained (wrapped) space}
\usage{
get_free_params(kernel)
}
\arguments{
\item{kernel}{A kernel object.}
}
\value{
A plain (unnamed) numeric vector, in tree-walk order.
}
\description{
Like \code{\link[=get_trainable_params]{get_trainable_params()}}, but returns each non-frozen
hyperparameter's \emph{wrapped} value (\verb{$wrapped}, the unconstrained space a
\link{parametrisation} maps to/from) instead of its constrained value -- no
\code{\link[=unwrap_param]{unwrap_param()}} is applied. Paired with \code{\link[=set_free_params]{set_free_params()}} for an
optimiser that works natively in unconstrained space (e.g. plain
gradient descent or \code{optim(method = "BFGS")} without box constraints):
round-tripping through \code{unwrap()}/\code{wrap()} on every iteration (the
constrained-space alternative) is not guaranteed bit-exact, since both
are floating-point transcendental functions (e.g. \code{log(exp(w))} need not
equal \code{w}) -- reading and writing \verb{$wrapped} directly avoids that drift
entirely.

The result is deliberately \strong{unnamed and positional}, unlike
\code{\link[=get_trainable_params]{get_trainable_params()}}: two hyperparameters sharing a bare name at
different points in the tree (see \code{\link[=.disambiguate_param_names]{.disambiguate_param_names()}}) are
still perfectly distinguishable here, by position, whereas a name-keyed
API would inherit the same ambiguity \code{\link[=kupdate]{kupdate()}} has (it updates \emph{every}
occurrence of a bare name at once). \code{\link[=set_free_params]{set_free_params()}} relies on this
same tree-walk order to write values back -- see its "Numerical safety"
section for a caveat about driving these values with an unbounded
optimiser.
}
\examples{
k <- se_kernel(length_scale = 2)
get_free_params(k)         # wrap(log_exp_parametrisation(), 2) == log(2)
get_trainable_params(k)    # 2 (the constrained value)
}
